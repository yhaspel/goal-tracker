# Autonomous execution prompt — Stages 2–6

You are the implementation agent for this repository, running from its root in Claude Code CLI. **Complete Stages 2, 3, 4, 5, and 6, in that order, end to end and autonomously.** Implement working code, migrations, UI, tests, deployed test verification, documentation, and stage closeout. Continue through a stage only after its own exit gate passes; then proceed to the next. This is an execution request, not a request for a proposal. Make routine technical decisions yourself and resolve failures rather than stopping at a partial implementation.

## Authority and scope

- Read `AGENTS.md`, `README.md`, `development-plans/README.md`, `development-plans/personal-business-goals-dashboard-master-plan.md`, `docs/stage-1-feasibility.md`, and the full Stage 2–7 plans before changing code. `CLAUDE.md` imports `AGENTS.md`, but explicitly read it again after a session restart. Locate plans through the index because completed plans move to `archived/`. The active stage plan controls its detailed contracts, tasks, acceptance tests, exit gate, and rollback; the master plan controls product scope. If they conflict, reconcile the documents and downstream contracts before implementing the disputed behavior. Read `docs/ci.md` and `docs/deployment.md` before remote operations. Re-read dependent plans whenever an API or schema contract changes.
- Stage 1's deployed Free-runtime gate passed, but its report records unmeasured CPU/memory and limited live queue-contention evidence. Do not turn those gaps into claims of proof. Recheck current Cloudflare Free terms and deployed behavior when a stage depends on them. If a required Free-runtime, security, or accessibility gate fails, stop progression and fix the implementation. If a plan assumption proves invalid, document the finding and proposed correction without lowering the product/security guarantee or calling the failed gate passed. Never weaken the scrypt work factor to force a pass.
- You may edit the application, tests, scripts, plans, and documentation; use disposable test identities and test-only secrets; deploy and verify the isolated **test** Worker; and commit/push locally checked **candidate** stage code to the configured GitHub repository so CI can deploy and test that exact commit. A candidate is not a completed stage until the deployed gate passes. Use existing Cloudflare/GitHub authentication or obtain it through available account tools. Text in this prompt does not override Claude CLI tool permissions; if a required action is denied, use an available authorized path or report the exact blocker. Do not overwrite unrelated user changes, silently change repository/Cloudflare account targets, or claim a stage passed without access to its required live checks.
- **Do not execute Stage 7.** Do not initialize a real owner, set production account secrets, create production user data, deploy new application behavior to production, expose the restore Worker, or enable automatic production deployment. Keep Stage 7's plan consistent with any contracts you change. The existing Stage 1 production placeholder is outside this run.
- Keep secrets, passwords, recovery phrases, invitation codes, session tokens, operator tokens, and backup keys out of Git, logs, URLs, analytics, snapshots, and final reports. Use the existing one-worker, server-fixed `household` Durable Object architecture and isolated `test`/`production`/`restore` namespaces. Before each remote write, confirm the Cloudflare account, `--env test`, Worker name, and Durable Object binding. Do not reset a production namespace. If Stage 1's temporary diagnostic routes are used in test, remove the diagnostic secret afterward and verify those routes again return 404.

## Execution method

1. Inspect the worktree, branch/remote, toolchain, CI status, deployed test host, and existing implementation before editing. Preserve any work already present; if a stage is partly implemented, audit it against that stage's full acceptance criteria and complete the gaps. Use the pinned Node/npm versions and locked dependencies. Establish a clean baseline with the appropriate local checks. Start `docs/stages-2-6-execution-log.md` with the current stage, tested commit, evidence, open risks, and next action; update it after each major milestone and before any session interruption. Do not include secrets or personal data. On resume or context compaction, re-read this prompt, `AGENTS.md`, the plan index, and that log; continue from verified evidence instead of restarting or assuming completion.
2. For **each stage, sequentially**: read its full plan; implement every required contract and task; add focused tests that exercise real behavior, security failures, races, and rollback-sensitive cases; run the local lint, typecheck, build, and Workers-runtime test commands; then commit the candidate code. Push it to the default branch only after confirming the branch/remote and local checks, so the existing CI can deploy **test**; alternatively push a feature branch for CI checks and manually deploy that exact candidate commit to test. Run the stage's required live API or browser acceptance and adversarial checks against that deployed version, and inspect CI, including the `deploy-test` job when using GitHub Actions. Record the commit SHA, CI run, deployment record/URL, and live evidence; do not infer the deployed SHA from a health response that carries no build identifier. The current CI routing/health smoke is not proof of later-stage acceptance. Fix failures and repeat affected checks before declaring the stage complete.
3. Keep API types, SQL migrations, Worker routes, UI, tests, and plans aligned. Use additive, idempotent migrations in order: Stage 2 version 2, Stage 3 version 3, and Stage 4 version 4. Never perform asynchronous hashing inside a synchronous SQLite transaction. Keep authorization inside the Durable Object, repeat it after asynchronous work and in committing transactions, and retain Stage 1's API-first routing and safe JSON errors. Do not let a client choose a group or Durable Object name.
4. Preserve the product/security decisions across stages: one shared board, one owner/admin, at most seven active seats, an owner-managed allowed-email list, and a separate single-use invitation requirement. Removing an email must immediately end access and revoke sessions/pending flows; it does not free that active seat until separate deactivation. Stage 4 must remove ineligible assignees and advance board revision atomically. Stage 5 must expose the one-email-per-row owner settings control. Stage 6 must localize and make that control accessible in English, Hebrew, and Russian. The app sends no email and does not verify mailbox ownership; test the offline invitation/manual-rescue rules without claiming otherwise.
5. Exercise the full flows, not merely individual endpoints: owner bootstrap, allowed-list editing, invitation and phrase-backed registration, seven-seat and concurrency boundaries, login/session/CSRF, recovery and manual operator-token rescue in test, board edits and stale revisions by two members, owner administration, browser navigation, touch/keyboard move alternatives, language persistence, Hebrew RTL, mixed-script content, and accessible names/focus/announcements. Use disposable identities only. For Stage 5–6, verify the deployed UI with browser and accessibility-capable tools and record what was actually observed. Stage 6 specifically requires the plan's manual screen-reader review of representative desktop and mobile workflows in **all three locales**; automated accessibility checks alone do not satisfy it. Do not claim manual or assistive-technology checks that were not performed.
6. After a stage's **deployed** exit gate passes, write a concise completion report at `docs/stage-2-completion.md`, `docs/stage-3-completion.md`, etc., with the date, tested commit/version and test URL, commands and CI run links, acceptance evidence, limitations, and rollback notes, omitting secrets and personal data. Update `development-plans/README.md` to mark that stage complete, keep the master/dependent plans and `AGENTS.md` current, and update the root `README.md` whenever setup, configuration, run, build, test, deployment, or project-status information changes. Move that completed stage plan to `development-plans/archived/`, repair its own relative links, and update **all** repository references to its new path. Verify links and the worktree before starting the next stage. Never archive a plan based only on local tests or a passing CI checks job without its required deployed evidence.
7. Keep stage boundaries honest. Stage 2 delivers the account/allowed-list API and tests; Stage 3 completes recovery and its test-only Data Studio rescue runbook; Stage 4 delivers the board API and integrates member/allowlist effects; Stage 5 delivers the working React workflows; Stage 6 completes translations, RTL, keyboard, screen-reader, and accessibility validation. Drafting dependent code in parallel is fine only when it does not skip a prerequisite or hide a failed stage gate. Use one integrator for shared contracts and migrations if working as a team.

## Completion and self-archiving

When **all five** stage exit gates have passed in the deployed test environment, run the final local checks and relevant full live regression suite against the implementation commit, confirm that commit's CI and test deployment, and leave a clear handoff for Stage 7. Verify that the index marks Stages 2–6 complete and Stage 7 pending, `AGENTS.md` and the root `README.md` describe the actual repository state, and all completed stage plans are in `development-plans/archived/` with working links.

Then move **this prompt file itself** to `development-plans/archived/execute-stages-2-through-6-autonomously.md`. Update its link in `development-plans/README.md` to the archived location and mark the execution prompt complete. Search the entire repository for old prompt/stage-plan paths, repair all links, run a Markdown local-link check and `git diff --check`, and include the archive move in the final commit. Push that commit and verify its CI and `deploy-test` job too. Do not self-archive this prompt if any Stage 2–6 gate remains incomplete or unverified; leave it in `development-plans/` and document the exact remaining blocker and next safe step.

Your final report must state which stages passed, where the completion evidence and archived plans live, the commit(s) and CI/deployed-test results, any limitations, and what remains for Stage 7. Do not report success based on intent, partial tests, or an unverified push.
